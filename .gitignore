node_modules/
logs/

# Archivos compilados del frontend - se generan con npm run build en el servidor
frontend/dist/

# Archivos temporales de exportación
laravel-api/export.sql

# Variables de entorno locales (nunca subir al repo)
.env
laravel-api/.env
frontend/.env
frontend/.env.local
